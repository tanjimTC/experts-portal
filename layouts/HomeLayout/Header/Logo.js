export const Logo = (props) => {
  return (
    <p
      className={`${props.color}`}
      style={{ fontSize: "25px", fontFamily: "Pacifico" }}
    >
      Experts Portal
    </p>
  );
};

export default Logo;
